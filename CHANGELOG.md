# Changelog

本项目 fork 自 [stuarthu/dsh-chrome](https://github.com/stuarthu/dsh-chrome) v0.1.3（MIT）。
以下记录本 fork 相对上游的全部变更。

## 0.1.5

### 新增（诊断）

排查「页面里选区明明存在、扩展却毫无反应且**不留任何痕迹**」这类故障时，原先只有
`content_ready` 一个信号 —— 它写在脚本最开头，脚本一开始跑就落了盘，**哪怕紧接着
就抛异常或变成孤儿也照样有记录**，于是区分不出下面三种情况：

| 情况 | 旧诊断能看出来吗 |
| --- | --- |
| 脚本压根没注入 | 能（无记录） |
| 注入了，但初始化中途抛异常（监听器没注册上） | **不能** |
| 注入了、初始化正常，但页面是孤儿（扩展重载后没刷新） | **不能** |

现在补成三个键，正好三分：

- `content_ready` —— 脚本开始执行（写在最前面，行为不变）
- `content_init` —— 初始化**跑完**、监听器均已注册（写在最末尾），并记录
  **扩展版本**与**是否孤儿**。这两项正是排查时最先要区分的：是旧版本还在跑？
  还是页面没跟着扩展一起刷新？
- `content_error` —— 初始化抛异常时的消息与栈顶（最多 3 行）

另外整个入口 IIFE 包了一层 try/catch（记录器定义在 IIFE 外面，否则接不住里面的
异常），所以以后再出现「初始化就挂了」的情况，直接读 `content_error` 即可，
不必再让用户去控制台手动取值。

### 测试

- `tests/test-frames.mjs` 增加 4 项：断言 `content_init` 被写入、且带上版本、
  孤儿状态与子帧标记。

## 0.1.4

### 修复

- **某些页面（实测为 Monaco 编辑器）划词毫无反应 —— 选区被我们自己丢掉了。**

  根因是抓取选区时对几何信息要求过严：

  ```js
  rect = selection.getRangeAt(0).getBoundingClientRect();
  if (rect.width === 0 && rect.height === 0) return undefined;  // 直接丢弃这次划词
  ```

  而这类编辑器里 Range 的几何是**退化**的，实测到过两种取值（同一页面不同时刻）：

  | 实测取值 | 旧代码的后果 |
  | --- | --- |
  | `{width: 0, height: 0}` | 命中判断 → **选区被丢弃**，连浮标都不建 → 看起来「扩展毫无反应」 |
  | `{width: 0, height: NaN}` | `NaN === 0` 为假，躲过判断 → 但 NaN 顺着 `Math.max(8, …)` 传下去，`left/top` 被写成 `"NaNpx"`（无效值被浏览器忽略）→ `position: fixed` 的浮标落在文档流末尾，**用户看不见** |

  两种取值都源于同一件事：编辑器用 CSS transform 做滚动/排版，嵌套下几何会算出 0 或 NaN。

  修复：几何信息坏掉**不再等同于「没有选区」** —— 它只影响浮标摆在哪。锚点矩形改为逐级退化：

  ```
  Range 包围盒 → Range 分段矩形(getClientRects) → 选区所在元素 → 鼠标位置 → 视口定点
  ```

  并且**每一级都要求四个数都是有限值、且不是空格子**（`Number.isFinite` 判定），
  保证写进 style 的永远是合法的 `NNNpx`，绝不会再出现 `NaNpx`。
  最后两档意味着：即使几何完全拿不到，这次划词也不会被丢掉，只是浮标位置不精确。

### 新增

- 诊断 `capture-miss`：当「确实存在非折叠选区、却仍未接住」时记录一次原因
  （文本长度、矩形宽高、range 数量），每页只记一次。普通点击不记，所以没有噪声。
  以后再遇到这类「选中了没反应」，可以直接从诊断里读出当时的几何值，不用再靠控制台追问。

### 测试

- 新增 `tests/test-capture-rect.mjs`（20 项），把上面两种实测取值各自钉成一个场景，
  并覆盖整条退化链（分段矩形 / 选区元素 / 指针 / 视口定点），逐条断言写进 style 的
  值合法且**不含 NaN**。

## 0.1.3

### 修复

- **iframe 里的划词完全没反应**。内容脚本原先第一行就是
  `if (window.top !== window) return;`，只在顶层帧运行。于是**正文嵌在 iframe 里的
  页面**（微前端外壳、后台系统、部分在线编辑器）划词彻底失效：选中高亮正常，但顶层
  document 既收不到 `mouseup` / `selectionchange`，`window.getSelection()` 也是空的
  —— 表现出来就是「有些网址划词没反应」，且没有任何报错可查。

  修复：manifest 改为 `all_frames: true`，并加 `match_about_blank` 覆盖
  `srcdoc` / `about:blank` 内嵌帧；脚本不再按帧提前退出。

  连带处理了两个只在「多帧」下才出现的问题：
  - 孤儿横幅（「扩展已更新，请刷新本页」）只在**顶层帧**弹一次，
    否则每个帧各弹一条，页面上会出现 N 条横幅；
  - 上报的**页面地址**：跨域帧读不到 `window.top.location`，退回
    `document.referrer` —— 它是嵌这个帧的页面，比帧自己的 URL 更接近用户所在的页面。
    同源帧直接用顶层帧 URL。

## 0.1.2

### 新增

- **侧栏未展开时，不再做解读**（针对「结果只进侧栏」的两种模式）。
  这两种模式的结果只在扩展侧栏显示，侧栏没打开时既看不到结果、也白跑一次模型。
  现在这种情况下整个跳过：不发请求，也不弹浮标。侧栏打开后立即恢复。

  两种「页面浮窗」模式（`click` / `auto`）**不受影响** —— 它们的结果画在页面上，
  本来就不需要侧栏。

  侧栏开合状态由 background 以侧栏那条长连接的有无判断，启动时告知内容脚本一次、
  之后主动推送。除了内容脚本源头的判断，background 侧还有一道兜底闸门，
  防的是「浮标弹出之后侧栏才被关掉」这类竞态。

### 变更

- 跳过时会在内容脚本诊断记录里留一条 `skip-sidebar-closed`（每页只记一次），
  免得以后出现「划词没反应」时无从判断。
- 设置页与 README 补充了该行为的说明。

## 0.1.1

### 修复

- **扩展安装目录不再多嵌套一层**。`dsh-insight install` 原先装到
  `<用户目录>/dsh-insight/extension/`，而「加载已解压的扩展程序」对话框里很自然
  会选中名字更像产品名的上一层 `<用户目录>/dsh-insight/`，于是报
  「清单文件丢失或不可读取」—— 那一层确实没有 `manifest.json`。现在直接装到
  `<用户目录>/dsh-insight/`，清单就在该目录根部，**选它即可**。
- `install` 增加了装后自检：若目标目录根部没有 `manifest.json`（例如目标已存在导致
  复制被套进子目录），直接报错退出，而不是打印一个错误路径。

扩展本体与 `manifest.json` 均未改动（仍为 0.1.0），此版本仅涉及安装器与文档。

## 0.1.0

首个公开版本。

### 新增

- **划词即解读**：选中文字后浮现浮标，点击即把选区文本与页面信息发给本机 dsh。
  四种触发与展示模式：`click`（默认）/ `click-sidebar` / `auto` / `auto-sidebar`。
- **侧栏原生多轮追问**：解读结果下方直接追问，携带最近 16 轮历史。
- **设置内嵌侧栏**：就地切换视图，不再跳转独立选项页；设置页懒加载。
- **侧栏外观可配置**：背景色与字号（11–20px）。
- **解读口径**：6 个内置口径；内置口径可编辑（覆盖层保存、可恢复默认），
  另可新建任意多个自定义口径。
- **零依赖 Markdown 渲染器**：不使用 `innerHTML`，支持标题/列表/引用/代码块/
  行内代码/加粗/链接（不支持表格）。
- **浮窗位置**：跟随选区或固定在右下角。
- **进度指示**：正在解读时显示指示条与已等待秒数。

### 修复

- **上下文溢出（400）**：页面自动注入会把整页正文当作常驻用户消息反复塞进
  最近活跃会话。当活动标签页正是 dsh 会话界面时，正文即整段对话，而对话中又
  含有先前注入的「当前页面」消息，于是自我复制、逐轮放大。上游 1,000,000 字符
  上限下实测 80 次注入约 246 万字符，把 104 万 token 的上下文顶到 150 万而报错。
- **划词请求在飞行途中丢弃新选区**：请求可能耗时 20 秒以上，此间的新选区会被
  静默丢弃，界面一直显示旧结果。改为排队覆盖（新者胜）并加了请求超时。
- **扩展更新后的孤儿内容脚本**：旧页面里的内容脚本失效时没有任何提示，划词看似
  无反应；现在会显示「已更新，请刷新本页」的提示条。
- **Markdown 渲染器死循环**：共享的全局正则 `lastIndex` 未复位导致无限递归，
  在大输入上会耗尽内存。
- 解读成功后结果不显示（`renderRecord` 漏了展开记录区的两行赋值）。
- 设置页在 `window.location` 不可用时抛错。

### 变更

- **页面自动注入默认关闭**，两层都关：宿主不挂载该插件行，扩展侧推送开关
  `insight_autopush` 默认 `false`。需要时两处一起开。
- 正文上限：扩展侧 `MAX_PAGE` 1,000,000 → **8,000**；
  桥接侧入站 `MAX_PAGE_CONTENT` 1,000,000 → **200,000**。
- 页面注入器改为有界实现：单次最多 4000 字符、命中自身回声（注入标记）整条跳过、
  单会话累计 60000 字符封顶。
- 移除 `cookies` 权限（已无消费者）。

### 更名

- 项目由 `dsh-chrome` 更名为 **`dsh-insight`**。包名、CLI（`dsh-insight install`）、
  扩展名、安装目录、桥接路由 `/dsh-insight/bridge`、桥接服务名 `dshInsightBridge`
  与插件行 id 一并更改。
- 扩展 `manifest.json` 的 `key` 保持不变，因此扩展 ID 不变、升级不丢已有设置。
- 上游 MIT 版权与许可声明按许可要求保留在 `LICENSE` 中，并补充了本 fork 的版权行。
